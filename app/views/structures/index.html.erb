<% content_for :title, "Strutture - Terminus" %>
<div class="container">
  <div class="row justify-content-center">
    <div class="col col-lg-8 p-2 m-2">
      <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-3">
        <h2>
          <%= image_tag("structures/structures_index.svg", class: "img-fluid me-3", alt: "structures_index") %>
          <span class="text-primary">
            <i>Le mie Strutture</i>
          </span>
        </h2>
        <%= link_to new_structure_path, class: "btn btn-primary" do %>
          <i class="bi bi-plus-lg"></i><i class="bi bi-building me-2"></i>Nuova Struttura
        <% end %>
      </div>
      <hr class="text-primary mt-3 mb-4"/>
      <div class="card border-0">
        <div class="card-body">
          <% if @structures.any? %>
            <div class="list-group">
              <% @structures.each do |structure| %>
                <div class="list-group-item d-flex justify-content-between align-items-center flex-wrap gap-2">
                  <div>
                    <span class="fs-4 fw-bold">
                      <%= structure.name %>
                    </span>
                  </div>
                  <div class="d-flex gap-2">
                    <%= link_to structure_path(structure), class: "btn btn-sm btn-info", data: { bs_toggle: "tooltip", bs_placement: "bottom" }, title: "Visualizza il Record Corrente" do %>
                      <i class="bi bi-eye"></i>
                    <% end %>
                    <%= link_to edit_structure_path(structure), class: "btn btn-sm btn-warning", data: { bs_toggle: "tooltip", bs_placement: "bottom" }, title: "Modifica il Record Corrente" do %>
                      <i class="bi bi-pencil"></i>
                    <% end %>
                    <%= link_to confirm_destroy_structure_path(structure), class: "btn btn-sm btn-danger", data: { bs_toggle: "tooltip", bs_placement: "bottom" }, title: "Elimina il Record Corrente" do %>
                      <i class="bi bi-trash"></i>
                    <% end %>
                  </div>
                </div>
              <% end %>
            </div>
          <% else %>
            <p class="card-text text-body-secondary mb-0">Non hai ancora nessuna struttura registrata.</p>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
