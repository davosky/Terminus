<%= form_with model: structure do |f| %>
  <% if structure.errors.any? %>
    <div class="alert alert-danger">
      <ul class="mb-0">
        <% structure.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="mb-3">
    <%= f.label :name, class: form_label_class_structure %>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="mb-3">
    <%= f.label :position, class: form_label_class_structure %>
    <%= f.number_field :position, class: "form-control" %>
  </div>
  <div class="d-flex gap-2">
    <%= f.button class: form_submit_accent_structure do %>
      <i class="bi bi-building me-2"></i><%= form_submit_label_structure %>
    <% end %>
    <%= link_to structures_path, class: "btn btn-secondary" do %>
      <i class="bi bi-arrow-counterclockwise"></i>
      &emsp;
      Annulla
    <% end %>
  </div>
<% end %>
